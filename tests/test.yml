---
# This is a test YAML file
env:
  linux: true

Ping: pong # Isn't this fun?

line-length-test: 'This is a very long line which should still pass in our linting tests since we have a default max line length as 160 characters.'
